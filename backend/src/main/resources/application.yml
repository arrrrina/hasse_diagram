spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/hasse_db
    username: postgres
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    open-in-view: false

jwt:
  secret: hasse-diagram-secret-key-change-in-production-minimum-256-bits-long
  expiration-ms: 86400000  # 24 hours

server:
  port: 8080
